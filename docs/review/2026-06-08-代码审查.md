# Auto Check-In 项目 —— 代码审查报告

日期：2026-06-08
审查范围：全部 Python 源文件（聚焦 v0.7.1 → 当前变更）
审查方式：全量逐文件审查

---

## 一、总体评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 架构设计 | ★★★★☆ | 模块划分清晰，CLI 与服务端分离，依赖方向正确 |
| 代码质量 | ★★★★☆ | 命名规范、函数短小，但存在 status_map 重复定义 |
| 安全性 | ★★★★☆ | 本次新增学号掩码，凭据展示已全面脱敏 |
| 错误处理 | ★★★★☆ | 打卡后增加了确认查询，但缺少 token 过期检测 |
| 可测试性 | ★★★☆☆ | 20 个工具函数测试，CLI 无测试覆盖 |
| 文档完整性 | ★★★★★ | AGENTS.md / CHANGELOG / CLAUDE.md / memory 完整同步 |
| 可维护性 | ★★★★☆ | 代码清晰，但 record 状态展示逻辑有重复 |

**较上次审查（2026-06-07）的变化：**
- ✅ 学号脱敏：3 处明文显示改为 `_mask(username, 3)`
- ✅ 打卡确认：`cmd_checkin` 提交后自动调用 `get_one_record` 核实状态
- ✅ 中文回复规范：CLAUDE.md / AGENTS.md 明确最终回复必须用中文

---

## 二、逐文件审查

### 2.1 `scripts/cli.py`（1217 行）

#### ✅ 正确项

| 项目 | 状态 |
|------|------|
| 学号掩码（3 处） | ✅ `_mask(username, 3)` 正确应用，前 3 后 3 脱敏 |
| 打卡后确认查询 | ✅ `get_one_record` 回查逻辑正确，状态颜色编码正确 |
| Style 终端系统 | ✅ 尊重 NO_COLOR + isatty，Windows/Unix 通用 |
| Spinner 动画 | ✅ daemon 线程，管道自动降级 |
| secure_input | ✅ 跨平台密码输入，回显 * 占位符 |
| 配置持久化 | ✅ JSON 存储，ensure_ascii=False 支持中文 |
| Token 过期检测 | ✅ `_token_expired` 统一校验 code=401 + msg 关键词 |
| 错误提示 | ✅ 每种错误场景有具体解决引导 |

#### ⚠️ 发现问题

| 优先级 | 行号 | 问题 | 建议 |
|--------|------|------|------|
| 🟡 P1 | 916-918 / 968-970 / 1045-1048 | `status_map` 在 `cmd_checkin`、`cmd_record`、`cmd_month` 三处重复定义，共 3 份相同字典 | 提取为模块级常量 `STATUS_MAP` |
| 🟡 P1 | 459-462 | `cmd_status` 的"今日打卡"状态显示只判断了"正常"/"已打卡"两种，没有使用完整的 status_map 做颜色编码（迟到/未归等不会着色） | 统一使用 STATUS_MAP 和颜色逻辑 |
| 🟢 P2 | 910 | `cmd_checkin` 确认查询 `get_one_record` 结果没有调用 `_token_expired()` 检查，若 token 恰好在提交后过期会显示模糊提示 | 增加 token 过期检测 |
| 🟢 P2 | 907-938 | `cmd_checkin` 的确认查询逻辑与 `cmd_record`（963-998）高度重复（status_map、颜色编码、字段展示），约 30 行重复代码 | 抽取共用函数 `_display_record(d)` |
| ⚪ P3 | 644 | `cmd_login`（密码登录）成功时只显示 `bullet("登录成功 ✓")`，没有像 `cmd_login_openid` 那样显示 masked 学号和 token | 补充显示 masked 学号和 token，保持一致 |
| ⚪ P3 | 408 | `_mask` 内部已处理空字符串返回 "(未设置)"，外层的 `if cfg.get("username") else` 是冗余防护 | 可保留（防御 None），也可简化为 `_mask(cfg.get("username") or "", 3)` |

---

### 2.2 `src/core/client.py`（193 行）

| 项目 | 状态 |
|------|------|
| 接口完整性 | ✅ 8 个 API 端点全部封装 |
| 签名/认证 | ✅ FlySource-sign + Basic Auth + Token 三位一体 |
| 微信伪装 | ✅ Referer / User-Agent / charset 正确 |
| 重试机制 | ✅ 指数退避（1s/2s/4s），仅重试网络/解析错误 |
| 上下文管理器 | ✅ `__enter__`/`__exit__` + 显式 `close()` |
| 环境变量 | ✅ AppID / Version / UA / BaseURL / 凭据 均可通过环境变量覆盖 |
| SSL | ✅ certifi + trust_env=False 解决 Windows 证书/代理问题 |

**无新问题。** 上次审查（v0.6.0）的所有 P0/P1 问题已全部修复。

---

### 2.3 `src/utils/sign.py`（28 行）

| 项目 | 状态 |
|------|------|
| 签名正确性 | ✅ 与 `scripts/sign.js` 交叉验证通过 |
| 环境变量 | ✅ CLIENT_ID/SECRET 优先从环境变量读取 |

**无问题。**

---

### 2.4 `src/utils/crypto.py`（18 行）

| 项目 | 状态 |
|------|------|
| MD5 | ✅ 标准实现，UTF-8 编码 |
| Base64 | ✅ 编解码往返正确 |

**无问题。**

---

### 2.5 `src/utils/geo.py`（28 行）

| 项目 | 状态 |
|------|------|
| Haversine | ✅ 公式正确，已知距离测试通过 |
| GPS 偏移 | ✅ 保留 6 位小数，seed 参数支持可重现测试 |

**无问题。**

---

### 2.6 `src/main.py`（39 行）

| 项目 | 状态 |
|------|------|
| FastAPI 生命周期 | ✅ lifespan 管理 ApiClient 连接池 |
| CORS | ✅ 已添加（开发阶段 allow_origins=["*"]） |
| 环境变量加载 | ✅ dotenv 在导入其他模块前加载 |

**无问题。** 上次审查的 P0 问题已全部修复。

---

### 2.7 `tests/`（20 个用例）

| 文件 | 用例数 | 覆盖范围 |
|------|--------|---------|
| `test_crypto.py` | 6 | MD5 空值/英文/Unicode、Base64 标准向量/二进制/往返 |
| `test_sign.py` | 6 | 签名格式/路径差异/token 差异/时间戳差异/空 token/Basic Auth 格式 |
| `test_geo.py` | 8 | Haversine 同点/赤道/极点/已知距离、偏移边界/种子重现/种子差异/小数位 |

**无问题。** 全部 20 个测试通过。

---

### 2.8 文档与配置

| 文件 | 状态 |
|------|------|
| `CLAUDE.md` | ✅ 架构、命令、约定完整 |
| `AGENTS.md` | ✅ 与代码同步，遗留问题表已清空 |
| `.env.example` | ✅ 所有变量有默认值和注释 |
| `requirements.txt` | ✅ 版本号全部锁定 |
| `.gitignore` | ✅ 覆盖 Python/IDE/OS/数据库/逆向产物 |

---

## 三、问题汇总

### 待修复（按优先级）

| 优先级 | 文件 | 行号 | 问题 | 状态 |
|--------|------|------|------|------|
| 🟡 P1 | `scripts/cli.py` | 916-918, 968-970, 1045-1048 | `status_map` 字典重复 3 次 | ❌ 待修复 |
| 🟡 P1 | `scripts/cli.py` | 459-462 | `cmd_status` 状态颜色编码不完整 | ❌ 待修复 |
| 🟢 P2 | `scripts/cli.py` | 910 | `cmd_checkin` 确认查询缺少 token 过期检测 | ❌ 待修复 |
| 🟢 P2 | `scripts/cli.py` | 907-938 / 963-998 | 打卡记录展示逻辑重复（~30 行） | ❌ 待修复 |
| ⚪ P3 | `scripts/cli.py` | 644 | `cmd_login` 成功未显示 masked 学号 | ❌ 待修复 |
| ⚪ P3 | `scripts/cli.py` | 408 | `_mask` 空值检查冗余 | ❌ 可选优化 |

### 已验证通过

- ✅ 20/20 pytest 测试通过
- ✅ 签名算法与 JS 参考实现一致
- ✅ 微信环境伪装头正确
- ✅ 学号/OpenID/密码/Token 显示均脱敏
- ✅ 打卡后自动回查服务器确认状态

---

## 四、建议优先修复顺序

1. **P1 — 提取 STATUS_MAP 常量**（影响 3 个函数，改一处处处生效）
2. **P1 — `cmd_status` 状态颜色统一**（用户最常使用的命令，体验影响大）
3. **P2 — 抽取 `_display_record()` 共用函数**（消除 ~30 行重复代码）
4. **P2 — `cmd_checkin` 确认查询增加 token 过期检测**
5. **P3 — 其他**
